name: Validate

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release-metadata:
    name: Release Metadata
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Verify version metadata
        run: |
          python - <<'PY'
          import json
          import pathlib
          import re
          import tomllib

          root = pathlib.Path('.')
          pyproject = tomllib.loads((root / 'pyproject.toml').read_text())
          manifest = json.loads(
              (root / 'custom_components/bindhome/manifest.json').read_text()
          )
          frontend = root / 'frontend'
          package = json.loads((frontend / 'package.json').read_text())
          package_lock = json.loads((frontend / 'package-lock.json').read_text())
          hacs = json.loads((root / 'hacs.json').read_text())

          versions = {
              'pyproject.toml': pyproject['project']['version'],
              'manifest.json': manifest['version'],
              'frontend/package.json': package['version'],
              'frontend/package-lock.json': package_lock['version'],
              'frontend/package-lock.json root package': package_lock['packages']['']['version'],
          }
          if len(set(versions.values())) != 1:
              raise SystemExit(f'Version metadata mismatch: {versions}')

          version = next(iter(versions.values()))
          if not re.fullmatch(r'\d+\.\d+\.\d+', version):
              raise SystemExit(f'Version is not SemVer-compatible: {version}')

          minimum_ha = hacs.get('homeassistant')
          if not isinstance(minimum_ha, str) or not re.fullmatch(
              r'\d{4}\.\d+\.\d+', minimum_ha
          ):
              raise SystemExit(
                  f'Invalid HACS Home Assistant minimum: {minimum_ha!r}'
              )

          if hacs.get('zip_release') is not True:
              raise SystemExit('hacs.json must enable zip_release for stable installs')
          if hacs.get('filename') != 'bindhome.zip':
              raise SystemExit('hacs.json filename must be bindhome.zip')

          print(f'BindHome version: {version}')
          print(f'Minimum Home Assistant: {minimum_ha}')
          print('HACS release asset: bindhome.zip')
          PY

      - name: Build and validate HACS release package
        run: python scripts/build_release_package.py --output /tmp/bindhome.zip

  pytest:
    name: Pytest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.14"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install .[dev]

      - name: Run pytest
        run: pytest

  ruff:
    name: Ruff Lint & Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.14"
          cache: "pip"

      - name: Install Ruff
        run: pip install ruff

      - name: Ruff Check
        run: ruff check .

      - name: Ruff Format Check
        run: ruff format --check .

  hassfest:
    name: Hassfest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: home-assistant/actions/hassfest@master

  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test

      - name: Build production bundle
        run: npm run build

      - name: Verify committed bundle is current
        run: git diff --exit-code -- ../custom_components/bindhome/panel/static/bindhome-panel.js
