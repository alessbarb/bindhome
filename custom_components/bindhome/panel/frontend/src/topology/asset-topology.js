import { LitElement, css, html, nothing } from "lit";
import { createBindHomeApi } from "../api/bindhome-api.js";
import { normalizeWsError } from "../api/normalize-ws-error.js";
import { relationPartitions } from "./relation-state.js";
import "./relation-editor.js";

export class BindHomeAssetTopology extends LitElement {
  static properties = { hass: { attribute: false }, t: { attribute: false }, asset: { attribute: false }, assets: { attribute: false }, areas: { attribute: false }, registry: { attribute: false }, onRefresh: { attribute: false }, onNavigate: { attribute: false }, _editing: { state: true }, _deleting: { state: true }, _error: { state: true }, _sync: { state: true }, _confirm: { state: true } };
  constructor() { super(); this.hass = null; this.t = (key) => key; this.asset = null; this.assets = []; this.areas = []; this.registry = {}; this.onRefresh = null; this.onNavigate = null; this._editing = false; this._deleting = null; this._error = null; this._sync = null; this._confirm = null; }
  _name(id) { const asset = this.assets.find((item) => item.id === id); return asset?.name ?? id; }
  _area(id) { const asset = this.assets.find((item) => item.id === id); return this.areas.find((area) => area.area_id === asset?.area_id)?.name ?? null; }
  async _delete(relation) { if (this._deleting) return; this._deleting = relation.id; this._error = null; try { await createBindHomeApi(this.hass).deleteRelation(relation.id); this._deleting = null; this._confirm = null; try { await this.onRefresh?.(); } catch { this._sync = this.t("topology.sync_warning"); } } catch (error) { this._deleting = null; this._error = normalizeWsError(error, this.t("topology.delete_error")).message; } }
  _navigate(id) { this.onNavigate?.(id); this.dispatchEvent(new CustomEvent("navigate-asset", { detail: id, bubbles: true, composed: true })); }
  _renderRelation(relation, outgoing) { const other = outgoing ? relation.target_asset_id : relation.source_asset_id; return html`<li><button class="neighbor" @click=${() => this._navigate(other)}><strong>${this._name(other)}</strong><span>${relation.relation_type}${this._area(other) ? ` · ${this._area(other)}` : ""}</span></button>${this._confirm === relation.id ? html`<span class="confirm" role="alertdialog" aria-label=${this.t("topology.confirm_delete")}><span>${this.t("topology.confirm_delete")}</span><button @click=${() => (this._confirm = null)}>${this.t("editor.cancel")}</button><button @click=${() => this._delete(relation)} ?disabled=${Boolean(this._deleting)}>${this.t("topology.delete")}</button></span>` : html`<button class="delete" @click=${() => (this._confirm = relation.id)} ?disabled=${Boolean(this._deleting)}>${this.t("topology.delete")}</button>`}</li>`; }
  render() { const { outgoing, incoming } = relationPartitions(this.registry?.relations ?? [], this.asset?.id); return html`<section class="topology"><header><div><h3>${this.t("topology.title")}</h3><p>${this.t("topology.helper")}</p></div><button @click=${() => (this._editing = true)} ?disabled=${this._editing}>${this.t("topology.add_relation")}</button></header>${this._editing ? html`<bindhome-relation-editor .hass=${this.hass} .t=${this.t} .asset=${this.asset} .assets=${this.assets} .areas=${this.areas} .registry=${this.registry} .onRefresh=${this.onRefresh} .onDone=${() => (this._editing = false)}></bindhome-relation-editor>` : nothing}${this._sync ? html`<p class="warning" role="alert">${this._sync}</p>` : nothing}${this._error ? html`<p class="error" role="alert">${this._error}</p>` : nothing}<div class="columns"><div><h4>${this.t("topology.outgoing")}</h4>${outgoing.length ? html`<ul>${outgoing.map((r) => this._renderRelation(r, true))}</ul>` : html`<p class="muted">${this.t("topology.no_relations")}</p>`}</div><div><h4>${this.t("topology.incoming")}</h4>${incoming.length ? html`<ul>${incoming.map((r) => this._renderRelation(r, false))}</ul>` : html`<p class="muted">${this.t("topology.no_relations")}</p>`}</div></div></section>`; }
  static styles = css`:host{display:block}.topology{padding:16px;border:1px solid var(--divider-color);border-radius:10px;background:var(--card-background-color)}header{display:flex;justify-content:space-between;gap:12px;align-items:start}h3,h4,p{margin:0}header p{color:var(--secondary-text-color);font-size:13px;margin-top:4px}.columns{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:20px;margin-top:16px}ul{list-style:none;padding:0;margin:8px 0;display:grid;gap:8px}li{display:flex;gap:8px;align-items:stretch}.neighbor{flex:1;text-align:left;padding:9px;border:1px solid var(--divider-color);background:transparent;border-radius:6px}.neighbor span{display:block;color:var(--secondary-text-color);font-size:12px}.delete{border:0;background:transparent;color:var(--error-color);cursor:pointer}.topology button{font:inherit;cursor:pointer}.error{color:var(--error-color)}.warning{color:var(--warning-color)}.muted{color:var(--secondary-text-color)}@media(max-width:600px){.columns{grid-template-columns:1fr}header{flex-direction:column}.delete{padding-inline:4px}}`;
}
customElements.define("bindhome-asset-topology", BindHomeAssetTopology);
